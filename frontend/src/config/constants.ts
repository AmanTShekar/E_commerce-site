/**
 * NEXMART Global Constants
 */

// In production, the API might be on a different domain or proxied via Cloudflare Pages /api route
export const API_BASE_URL = import.meta.env.VITE_API_URL || 'https://nexmart-backend.aman-tshekar.workers.dev/api';

export const SITE_NAME = 'NEXMART';
export const SITE_DESCRIPTION = 'High-fidelity hardware marketplace for the modern artisan.';
